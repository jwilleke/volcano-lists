{
  "name": "template-tools",
  "version": "1.0.0",
  "description": "Utilities for smart template integration",
  "private": true,
  "scripts": {
    "merge": "ts-node merge-template.ts",
    "merge:dry-run": "ts-node merge-template.ts --dry-run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
